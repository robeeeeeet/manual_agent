# Skill再レビュー: `manual-ai-processing`（v2）

対象: `.claude/skills/manual-ai-processing/`

## 改善が確認できた点

- **SDK統一**: `google-genai` 採用を明言し、参照側（`references/gemini-api-patterns.md`）も追従している。
- **抽出パイプライン（必須3段階）**と **不確実性の扱い（標準ルール）** が追加され、出力のブレが減る。
- **DoD**（代表画像/代表PDF、構造化エラー、スキーマ検証）が具体化され、完了の判断がしやすい。

## 残っている課題（優先度順）

1. **（中）例コードの“コピペ即実行性”**
   - サンプルによって `import time` や `prompt` 等の前提が省略されており、初学者がそのまま動かすと詰まる可能性がある。

## 追加で良くなる提案

- **（低）最小の動作サンプルを1つに集約**
  - 画像1枚→JSON抽出→Pydantic検証→正規化、までを1つの短いスクリプト例にまとめると強い。
